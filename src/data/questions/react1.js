import { M, F } from './helpers.js'

// Days 29–42: React fundamentals. 10 questions per day (7 multiple choice + 3 fill in the blank).
export const react1 = {
  29: [
    M('What does JSX compile down to?', ['HTML strings', 'Function calls that create element objects', 'Templates', 'Web components'], 1, 'JSX', 'medium'),
    M('How do you embed a JavaScript expression in JSX?', ['{{ x }}', '{ x }', '${x}', '<%= x %>'], 1, 'JSX expressions', 'easy'),
    M('Which attribute name does JSX use instead of `class`?', ['cssClass', 'className', 'classList', 'styleClass'], 1, 'JSX attributes', 'easy'),
    M('What is the virtual DOM?', ['A browser API', 'An in-memory representation React diffs against', 'A faster HTML parser', 'A shadow DOM'], 1, 'Virtual DOM', 'easy'),
    M('What must a component return if rendering multiple siblings without a wrapper div?', ['An array only', 'A Fragment (<>...</>)', 'null', 'A string'], 1, 'Fragments', 'easy'),
    M('Comparing the new virtual tree to the previous one is called...', ['Hydration', 'Reconciliation', 'Rendering', 'Batching'], 1, 'Reconciliation', 'medium'),
    M('Why can\'t JSX contain an `if` statement inline?', ['Syntax limits', 'JSX embeds expressions, not statements', 'Performance', 'It can'], 1, 'JSX expressions', 'hard'),
    F('JSX elements must have exactly one ___ element (or a Fragment).', 'root', 'JSX', 'easy'),
    F('React updates only the parts of the real DOM that ___ between renders.', 'changed', 'Virtual DOM', 'medium'),
    F('In JSX the `for` attribute of a label is written ___.', 'htmlFor', 'JSX attributes', 'hard'),
  ],
  30: [
    M('How does data flow between React components?', ['Both directions', 'Down via props', 'Up via props', 'Through globals'], 1, 'Data flow', 'easy'),
    M('Can a child component modify its props?', ['Yes, freely', 'No — props are read-only', 'Only objects', 'Only with setProps'], 1, 'Props immutability', 'easy'),
    M('How must a React component function be named?', ['camelCase', 'Starting with a capital letter', 'With "use"', 'Any name'], 1, 'Components', 'easy'),
    M('How do you pass a number 5 as a prop?', ['count="5"', 'count={5}', 'count=5', 'count:(5)'], 1, 'Props', 'easy'),
    M('How does a child communicate an event to a parent?', ['Modifying props', 'Calling a callback prop', 'Global state', 'Re-rendering'], 1, 'Lifting state', 'medium'),
    M('What is prop drilling?', ['Type-checking props', 'Passing props through many intermediate layers', 'Spreading props', 'Debugging props'], 1, 'Prop drilling', 'medium'),
    M('What does `<Card {...props} />` do?', ['Clones Card', 'Spreads all properties as individual props', 'Passes props as one object prop', 'Errors'], 1, 'Spread props', 'medium'),
    F('A component that receives only props and renders the same output each time is called ___.', 'pure', 'Pure components', 'hard'),
    F('Moving shared state to the closest common ancestor is called ___ state up.', 'lifting', 'Lifting state', 'medium'),
    F('Default values for missing props can be set with default ___ in the destructured signature.', 'parameters', 'Props', 'hard'),
  ],
  31: [
    M('What does `useState(0)` return?', ['The value 0', 'An array: [value, setter]', 'An object', 'A setter only'], 1, 'useState', 'easy'),
    M('What happens when you call a state setter?', ['Nothing until refresh', 'React re-renders the component', 'The DOM updates instantly', 'The variable mutates'], 1, 'Re-rendering', 'easy'),
    M('Why is `count++` wrong for React state?', ['Syntax error', 'Mutating state directly won\'t trigger a render', 'Too slow', 'It works fine'], 1, 'State immutability', 'easy'),
    M('When should you use the functional form `setCount(c => c + 1)`?', ['Never', 'When the new state depends on the previous', 'Only for objects', 'Only in effects'], 1, 'Functional updates', 'medium'),
    M('How do you update one field of an object in state?', ['state.field = x', 'setState({ ...state, field: x })', 'setState(field)', 'Object.assign(state)'], 1, 'Object state', 'medium'),
    M('Calling `setCount(1)` twice with the same value causes...', ['Two renders', 'React to skip the re-render', 'An error', 'A loop'], 1, 'Bailout', 'hard'),
    M('When is state initialized from the `useState` argument?', ['Every render', 'Only on the first render', 'On every update', 'When it changes'], 1, 'useState', 'medium'),
    F('Passing a function to useState, e.g. `useState(() => expensive())`, is called ___ initialization.', 'lazy', 'Lazy initialization', 'hard'),
    F('State updates in React event handlers are ___ together into a single re-render.', 'batched', 'Batching', 'medium'),
    F('Each component instance gets its own independent copy of ___.', 'state', 'useState', 'easy'),
  ],
  32: [
    M('When does `useEffect(fn, [])` run?', ['Every render', 'Only after the first render', 'Before render', 'Never'], 1, 'Effect dependencies', 'easy'),
    M('What does the function returned from an effect do?', ['Runs first', 'Cleans up before re-run and on unmount', 'Cancels the render', 'Nothing'], 1, 'Cleanup', 'medium'),
    M('When does `useEffect(fn)` with NO dependency array run?', ['Once', 'After every render', 'Never', 'On unmount'], 1, 'Effect dependencies', 'easy'),
    M('What belongs in the dependency array?', ['All variables', 'Every reactive value the effect reads', 'Only props', 'Only state'], 1, 'Effect dependencies', 'medium'),
    M('What commonly causes an infinite effect loop?', ['Empty deps', 'Setting state the effect depends on without conditions', 'Cleanup functions', 'Async effects'], 1, 'Effect pitfalls', 'medium'),
    M('Why can\'t the effect callback itself be async?', ['Syntax error', 'It must return a cleanup function, not a promise', 'Effects are sync-only internally', 'It can be'], 1, 'Async effects', 'hard'),
    M('When does effect cleanup run on unmount?', ['Before removal from the DOM tree completes', 'Never', 'After a delay', 'Only with deps'], 0, 'Cleanup', 'easy'),
    F('Subscribing to events in an effect requires removing them in the ___ function.', 'cleanup', 'Cleanup', 'easy'),
    F('useEffect runs ___ the browser paints, not during rendering.', 'after', 'Effect timing', 'hard'),
    F('Fetching data in an effect should handle stale responses with a cleanup ___ or AbortController.', 'flag', 'Async effects', 'hard'),
  ],
  33: [
    M('Which prop attaches a click handler in React?', ['onclick', 'onClick', 'on-click', 'handleClick'], 1, 'Event props', 'easy'),
    M('What is wrong with `onClick={handleClick()}`?', ['Nothing', 'It calls the function during render', 'Missing this', 'Wrong casing'], 1, 'Handler pitfalls', 'medium'),
    M('How do you pass an argument to a handler?', ['onClick={fn(id)}', 'onClick={() => fn(id)}', 'onClick={fn[id]}', 'arg={id}'], 1, 'Handler arguments', 'easy'),
    M('What does `e.preventDefault()` do in a form submit handler?', ['Stops propagation', 'Stops the browser\'s page reload', 'Clears the form', 'Validates inputs'], 1, 'preventDefault', 'easy'),
    M('What does `e.stopPropagation()` prevent?', ['Default behavior', 'The event bubbling to ancestors', 'Re-renders', 'Multiple clicks'], 1, 'Event bubbling', 'medium'),
    M('React wraps native events in a...', ['Proxy', 'SyntheticEvent', 'CustomEvent', 'Portal'], 1, 'Synthetic events', 'hard'),
    M('Which prop fires when an input\'s value changes?', ['onInput only', 'onChange', 'onUpdate', 'onType'], 1, 'Event props', 'easy'),
    F('Inside an input\'s onChange handler, the new text is read from `e.___.value`.', 'target', 'Event objects', 'easy'),
    F('Events in the DOM travel upward from the target to its ancestors — this is called ___.', 'bubbling', 'Event bubbling', 'medium'),
    F('`onKeyDown` handlers often check `e.___` === "Enter" to submit on the Enter key.', 'key', 'Keyboard events', 'hard'),
  ],
  34: [
    M('What does `{isOn && <Badge />}` render when isOn is false?', ['<Badge />', 'Nothing', 'false as text', 'An error'], 1, 'Logical AND rendering', 'easy'),
    M('Which pattern chooses between two elements?', ['if/else in JSX', 'A ternary: cond ? <A /> : <B />', 'switch in JSX', 'Loops'], 1, 'Ternary rendering', 'easy'),
    M('What does returning `null` from a component do?', ['Errors', 'Renders nothing', 'Unmounts the app', 'Renders "null"'], 1, 'Rendering nothing', 'easy'),
    M('Why is `{count && <List />}` risky when count is 0?', ['It renders the number 0', 'It crashes', 'It renders the list', 'It loops'], 0, 'Logical AND pitfalls', 'hard'),
    M('Where can you use if/else for conditional JSX?', ['Inside the JSX braces', 'Before the return statement', 'Nowhere', 'Only in classes'], 1, 'Conditional rendering', 'medium'),
    M('How do you conditionally apply a CSS class?', ['class-if', 'className={active ? "on" : "off"}', 'style.if', 'v-if'], 1, 'Conditional classes', 'easy'),
    M('A common pattern while data loads is to render...', ['null forever', 'A loading state, then the data', 'The stale data', 'An error'], 1, 'Loading states', 'easy'),
    F('`{error ? <Error /> : <Data />}` uses the ___ operator for two-way branching.', 'ternary', 'Ternary rendering', 'medium'),
    F('Guarding with `if (!user) return null` early in a component is called an early ___.', 'return', 'Early returns', 'medium'),
    F('Rendering different UIs for loading, error, and success models the request\'s three ___.', 'states', 'Loading states', 'hard'),
  ],
  35: [
    M('Which method renders an array of items in JSX?', ['forEach', 'map', 'filter', 'reduce'], 1, 'Rendering lists', 'easy'),
    M('What prop must each list item have?', ['id', 'key', 'index', 'ref'], 1, 'Keys', 'easy'),
    M('What does React use keys for?', ['Styling', 'Matching items across renders', 'Sorting', 'Validation'], 1, 'Keys', 'medium'),
    M('Why is using the array index as a key risky?', ['Slow', 'Reordering items confuses React\'s matching', 'Not allowed', 'Keys must be strings'], 1, 'Index keys', 'medium'),
    M('What makes a good key?', ['Math.random()', 'A stable unique id from the data', 'The item text', 'The index always'], 1, 'Keys', 'easy'),
    M('Where should the key prop go?', ['On the innermost element', 'On the element returned directly by map', 'On the parent list', 'Anywhere'], 1, 'Keys', 'hard'),
    M('Can a component read its own `key` prop?', ['Yes', 'No — React consumes it', 'Only in effects', 'Only strings'], 1, 'Keys', 'hard'),
    F('Keys must be ___ among siblings, but not globally.', 'unique', 'Keys', 'easy'),
    F('Rendering `items.map(item => <Row />)` without a key logs a ___ in the console.', 'warning', 'Keys', 'easy'),
    F('Changing a component\'s key forces React to ___ it, resetting its state.', 'remount', 'Keys', 'medium'),
  ],
  36: [
    M('What defines a controlled input?', ['Uses refs', 'Its value comes from state and updates via onChange', 'No handlers', 'Native validation'], 1, 'Controlled inputs', 'easy'),
    M('Which two props make an input controlled?', ['id and name', 'value and onChange', 'ref and value', 'defaultValue and onInput'], 1, 'Controlled inputs', 'easy'),
    M('What is an uncontrolled input?', ['Broken input', 'The DOM holds the value; read it via a ref', 'A hidden input', 'Read-only input'], 1, 'Uncontrolled inputs', 'medium'),
    M('Which prop sets an initial value on an UNcontrolled input?', ['value', 'defaultValue', 'initial', 'start'], 1, 'Uncontrolled inputs', 'medium'),
    M('How is a checkbox controlled?', ['value prop', 'checked prop with onChange', 'selected prop', 'on prop'], 1, 'Checkboxes', 'medium'),
    M('How do you handle many form fields with one state object?', ['One useState each', 'A single object keyed by input name', 'Global variables', 'Refs only'], 1, 'Form state', 'easy'),
    M('Setting `value` without `onChange` makes the input...', ['Uncontrolled', 'Read-only (with a warning)', 'Invalid', 'Focused'], 1, 'Controlled inputs', 'hard'),
    F('In React, a `<select>` is controlled with the ___ prop instead of a selected attribute.', 'value', 'Select inputs', 'hard'),
    F('`setForm({ ...form, [e.target.___]: e.target.value })` updates a field by its name attribute.', 'name', 'Form state', 'medium'),
    F('With controlled inputs, React state is the single source of ___.', 'truth', 'Controlled inputs', 'easy'),
  ],
  37: [
    M('What does the `children` prop contain?', ['Child state', 'Whatever is nested between the component\'s tags', 'An array always', 'DOM nodes'], 1, 'children prop', 'easy'),
    M('Which pattern does React recommend over inheritance?', ['Mixins', 'Composition', 'Globals', 'Cloning'], 1, 'Composition', 'easy'),
    M('What is a layout component like `<Card>{content}</Card>` an example of?', ['Prop drilling', 'Containment via children', 'Context', 'A portal'], 1, 'Containment', 'easy'),
    M('How do you pass multiple distinct "slots" of JSX?', ['Multiple children', 'Separate props like header={<H />} footer={<F />}', 'Arrays only', 'Portals'], 1, 'Slot props', 'medium'),
    M('What is a render prop?', ['A style prop', 'A function prop the component calls to render content', 'A key', 'A ref'], 1, 'Render props', 'hard'),
    M('A wrapper that adds behavior around any children is called a...', ['Portal', 'Wrapper/container component', 'Hook', 'Reducer'], 1, 'Composition', 'easy'),
    M('What can `children` be?', ['Only elements', 'Elements, strings, numbers, arrays — any renderable', 'Only one element', 'Only components'], 1, 'children prop', 'medium'),
    F('`<Modal><p>Hi</p></Modal>` — inside Modal, the paragraph arrives via the ___ prop.', 'children', 'children prop', 'easy'),
    F('Building complex UIs by nesting simple components is called ___.', 'composition', 'Composition', 'medium'),
    F('Specializing a generic Button into a DangerButton by wrapping it and pre-setting props is composition, not ___.', 'inheritance', 'Composition', 'hard'),
  ],
  38: [
    M('What problem does Context solve?', ['Slow renders', 'Prop drilling through many layers', 'Data fetching', 'Routing'], 1, 'Context purpose', 'easy'),
    M('Which function creates a context?', ['useContext()', 'createContext()', 'new Context()', 'makeContext()'], 1, 'createContext', 'easy'),
    M('How does a component read a context value?', ['this.context', 'useContext(MyContext)', 'MyContext.get()', 'props.context'], 1, 'useContext', 'easy'),
    M('Which component supplies the context value to a subtree?', ['Consumer', 'Provider', 'Supplier', 'Root'], 1, 'Provider', 'easy'),
    M('When do context consumers re-render?', ['Never', 'When the provider\'s value changes', 'Every render', 'On mount only'], 1, 'Context re-renders', 'medium'),
    M('What does a consumer get with no provider above it?', ['undefined always', 'The default value from createContext', 'null', 'An error'], 1, 'Default values', 'medium'),
    M('Why can `value={{ user }}` cause extra re-renders?', ['Objects are slow', 'A new object identity is created each render', 'Context is broken', 'It can\'t'], 1, 'Context re-renders', 'hard'),
    F('Common context use cases include the current theme, locale, and authenticated ___.', 'user', 'Context use cases', 'easy'),
    F('Wrapping the provider in a custom component like `<ThemeProvider>` plus a `useTheme` hook is a common context ___.', 'pattern', 'Context patterns', 'hard'),
    F('To avoid new-object churn, wrap the provider value in ___ so its identity is stable.', 'useMemo', 'Context re-renders', 'medium'),
  ],
  39: [
    M('What does `useRef(initial)` return?', ['The value', 'An object with a .current property', 'A DOM node', 'A setter'], 1, 'useRef', 'easy'),
    M('What happens when you change `ref.current`?', ['A re-render', 'Nothing — no re-render is triggered', 'An error', 'A DOM update'], 1, 'Refs vs state', 'medium'),
    M('How do you attach a ref to a DOM element?', ['id={ref}', 'ref={myRef}', 'dom={myRef}', 'bind={myRef}'], 1, 'DOM refs', 'easy'),
    M('When is `ref.current` set to the DOM node?', ['During render', 'After the element is mounted', 'Before render', 'On click'], 1, 'Ref timing', 'medium'),
    M('Which is a classic useRef use case?', ['Storing form state', 'Focusing an input imperatively', 'Fetching data', 'Styling'], 1, 'DOM refs', 'easy'),
    M('What else are refs good for besides DOM access?', ['Nothing', 'Mutable values that persist across renders without re-rendering', 'Props', 'Context'], 1, 'Instance variables', 'medium'),
    M('How do you store an interval id so cleanup can clear it?', ['In state', 'In a ref', 'In a global', 'In props'], 1, 'Instance variables', 'hard'),
    F('`inputRef.current.___()` gives keyboard focus to the input element.', 'focus', 'DOM refs', 'easy'),
    F('Reading or writing refs during ___ is discouraged; do it in effects or handlers.', 'rendering', 'Ref timing', 'hard'),
    F('Unlike state, updating a ref does not cause a ___.', 're-render', 'Refs vs state', 'easy'),
  ],
  40: [
    M('What does `useMemo(fn, deps)` cache?', ['The function', 'The computed return value', 'The component', 'The props'], 1, 'useMemo', 'easy'),
    M('What does `useCallback(fn, deps)` cache?', ['The return value', 'The function identity itself', 'The args', 'The render'], 1, 'useCallback', 'medium'),
    M('When does useMemo recompute?', ['Every render', 'When a dependency changes', 'Never', 'On unmount'], 1, 'Dependencies', 'easy'),
    M('Why memoize a callback passed to a memoized child?', ['Speed of the call', 'A stable identity prevents the child re-rendering', 'Required by React', 'Avoids closures'], 1, 'useCallback', 'medium'),
    M('What does `React.memo(Component)` do?', ['Caches all renders', 'Skips re-render when props are shallow-equal', 'Freezes props', 'Memoizes state'], 1, 'React.memo', 'medium'),
    M('When is useMemo overkill?', ['Expensive computations', 'Cheap calculations run on every render', 'Referential stability', 'Large lists'], 1, 'When to memoize', 'easy'),
    M('`useCallback(fn, deps)` is equivalent to...', ['useMemo(() => fn, deps)', 'useMemo(fn, deps)', 'memo(fn)', 'useRef(fn)'], 0, 'useCallback', 'hard'),
    F('Memoization trades ___ for CPU time by caching previous results.', 'memory', 'Memoization tradeoffs', 'hard'),
    F('`React.memo` compares props with a ___ equality check by default.', 'shallow', 'React.memo', 'hard'),
    F('Without useCallback, a function defined in a component gets a new ___ every render.', 'identity', 'useCallback', 'medium'),
  ],
  41: [
    M('What must a custom hook\'s name start with?', ['hook', 'use', 'my', 'on'], 1, 'Hook naming', 'easy'),
    M('What is a custom hook?', ['A component', 'A function that reuses stateful logic via other hooks', 'A class', 'A plugin'], 1, 'Custom hooks', 'easy'),
    M('Do two components using the same hook share state?', ['Yes', 'No — each call gets isolated state', 'Only objects', 'Only with context'], 1, 'Hook isolation', 'medium'),
    M('Which rule governs where hooks can be called?', ['Anywhere', 'Top level of components/hooks only, never in conditions or loops', 'Only in effects', 'Only once'], 1, 'Rules of hooks', 'medium'),
    M('What would a `useLocalStorage` hook typically return?', ['Nothing', 'A [value, setValue] pair like useState', 'A DOM node', 'A promise'], 1, 'Custom hook design', 'easy'),
    M('Why extract logic into a custom hook?', ['Faster renders', 'Reuse and cleaner components', 'Required by React', 'Smaller bundles'], 1, 'Custom hooks', 'easy'),
    M('Why must hooks not be called conditionally?', ['Style rule', 'React tracks hooks by call order each render', 'Performance', 'Async issues'], 1, 'Rules of hooks', 'hard'),
    F('A `useFetch(url)` hook typically returns data, loading, and ___ states.', 'error', 'Custom hook design', 'easy'),
    F('Custom hooks can call other hooks, including other ___ hooks.', 'custom', 'Custom hooks', 'medium'),
    F('The ESLint plugin that enforces the rules of hooks is eslint-plugin-react-___.', 'hooks', 'Rules of hooks', 'hard'),
  ],
  42: [
    M('Which component maps a URL path to an element in React Router?', ['<Page>', '<Route>', '<Path>', '<View>'], 1, 'Routes', 'easy'),
    M('Which component should you use for internal navigation links?', ['<a>', '<Link>', '<Nav>', '<Go>'], 1, 'Link', 'easy'),
    M('Why use <Link> instead of <a>?', ['Styling', 'It navigates without a full page reload', 'Security', 'SEO'], 1, 'Client-side routing', 'easy'),
    M('Which hook reads URL parameters like /users/:id?', ['useQuery()', 'useParams()', 'useUrl()', 'useRoute()'], 1, 'URL params', 'easy'),
    M('Which hook navigates programmatically?', ['useHistory() only', 'useNavigate()', 'useRedirect()', 'useGo()'], 1, 'Programmatic navigation', 'medium'),
    M('What path matches any unmatched URL for a 404 page?', ['path="404"', 'path="*"', 'path="missing"', 'default'], 1, '404 routes', 'medium'),
    M('How do nested routes render their child content?', ['children prop', 'An <Outlet /> in the parent', 'Slots', 'Portals'], 1, 'Nested routes', 'hard'),
    F('The `:id` part of path="/users/:id" is called a dynamic ___.', 'segment', 'URL params', 'medium'),
    F('`useNavigate()(-1)` navigates ___ one entry in the history stack.', 'back', 'Programmatic navigation', 'hard'),
    F('All routes must be wrapped in a router component such as `<___Router>`.', 'Browser', 'Router setup', 'medium'),
  ],
}
